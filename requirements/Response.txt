### AI GENERATION RESPONSE SUMMARY ###

--- FEATURE: amazon_smartphone.feature ---
Feature: Purchase a smartphone under 30000 on Amazon by applying filters

  As a price-conscious shopper,
  I want to find and purchase a smartphone under 30000 on Amazon
  by filtering products based on price, ratings, and reviews,
  so that I can compare top-rated options within my budget and make a confident purchase.

  Background:
    Given I am on the Amazon homepage

  Scenario: Navigate to Mobiles - Smartphones category
    When I navigate to Mobiles - Smartphones
    Then I should see the smartphone listings page

  Scenario: Apply price filter for smartphones up to 30000
    Given I am on the Smartphones listing page
    When I apply a price filter of Up to 30000
    Then only smartphones priced at or below 30000 are shown

  Scenario: Verify product count updates after applying price filter
    Given the price filter is applied
    When the results reload
    Then the number of products updates accordingly

  Scenario: Validate each filtered smartphone price before coupons
    Given filtered products are displayed
    Then each product has a listed price less than or equal to 30000 (before coupons)

--- STEP DEFINITIONS ---
# Reused existing step for: When I apply a price filter of Up to 30000
from behave import then

@then('each product has a listed price less than or equal to 30000 (before coupons)')
def step_impl(context):
    # Assuming context.page has a method to verify product prices
    assert context.page.all_products_price_at_most(30000), \
        "Not all products have a listed price <= 30000 (before coupons)"

--- PAGE METHODS ---
def apply_price_filter_up_to_30000(self):
    price_filter_locator = "[data-testid='price-filter-up-to-30000']"
    self.click(price_filter_locator)
# Use existing method: search_to_checkout

--- FEATURE: user_story.feature ---
Feature: ATM Cash Withdrawal
  As a bank customer,
  I want to withdraw cash from an ATM,
  So that I can have physical currency.

  Background:
    Given I have inserted my card

  Scenario: Successful cash withdrawal with sufficient balance and receipt requested
    Given I enter a valid PIN "1234"
    When I select to withdraw an amount of 500
    And the system verifies sufficient balance
    Then the system dispenses cash of 500
    And the system updates my account balance accordingly
    And I request a receipt
    Then I receive a receipt for the transaction

  Scenario: Cash withdrawal denied due to insufficient balance
    Given I enter a valid PIN "1234"
    When I select to withdraw an amount of 500
    And the system verifies insufficient balance
    Then the system denies the withdrawal
    And I do not receive cash

  Scenario: Card insertion and invalid PIN entry lockout after 3 failed attempts
    Given I enter an invalid PIN "0000"
    And I enter an invalid PIN "1111"
    And I enter an invalid PIN "2222"
    Then my account is locked for 15 minutes
    And the system denies further withdrawal attempts

  Scenario: User requests cash withdrawal without requesting receipt
    Given I enter a valid PIN "1234"
    When I select to withdraw an amount of 500
    And the system verifies sufficient balance
    Then the system dispenses cash of 500
    And the system updates my account balance accordingly
    And I do not request a receipt
    Then I do not receive a receipt

--- STEP DEFINITIONS ---
from behave import given

@given('I have inserted my card')
def step_impl(context):
    # Implement the logic to simulate card insertion
    # e.g., context.atm.insert_card()
    pass
from behave import given

@given('I enter a valid PIN "{pin}"')
def step_enter_valid_pin(context, pin):
    # Implement the action of entering a valid PIN
    context.atm_machine.enter_pin(pin)
@when('I select to withdraw an amount of 500')
def step_select_withdraw_amount_500(context):
    context.user.select_withdrawal_amount("500")
# Reused existing step for: And the system verifies sufficient balance
@then('the system dispenses cash of {amount:d}')
def step_impl(context, amount):
    context.system.dispense_cash(amount)
# Reused existing step for: And the system updates my account balance accordingly
# Reused existing step for: And I request a receipt
@then('I receive a receipt for the transaction')
def step_impl(context):
    # Implement verification that a receipt was received after the transaction
    # Assuming there's a page object or context attribute with a method to check receipt
    assert context.page.system_provides_receipt(), "Receipt was not received for the transaction"
@given('I enter a valid PIN "{pin}"')
def step_impl(context, pin):
    context.user.enter_pin(pin)
# Reused existing step for: When I select to withdraw an amount of 500
@then('the system verifies insufficient balance')
def step_impl(context):
    # Implementation depends on the application under test
    # For example, check an error message or state indicating insufficient balance
    assert context.system.has_insufficient_balance()  # Placeholder for actual verification
@then('the system denies the withdrawal')
def step_impl(context):
    # Implementation depends on how the system indicates denial of withdrawal
    # For example, checking for an error message or status flag
    assert context.system.is_withdrawal_denied(), "The system did not deny the withdrawal as expected"
@then('I do not receive cash')
def step_impl(context):
    # Implement verification that cash was not received
    # This assumes there is a method or attribute to check cash dispensing status in context
    assert not getattr(context, 'cash_dispensed', False), "Cash was received but should not have been"
@given('I enter an invalid PIN "{pin}"')
def step_enter_invalid_pin(context, pin):
    # Implement entering an invalid PIN
    # Assuming a method context.atm.enter_pin exists; else placeholder
    context.atm.enter_pin(pin)
    context.atm.verify_pin_invalid()
@when('I enter an invalid PIN "{pin}"')
def step_enter_invalid_pin(context, pin):
    # Implementation for entering an invalid PIN
    # Assuming there is a method in the page object to enter pin
    context.page.enter_pin(pin)
@when('I enter an invalid PIN "{pin}"')
def step_enter_invalid_pin(context, pin):
    # Implement entering the invalid PIN here
    # Example:
    context.page.enter_pin(pin)
@then('my account is locked for 15 minutes')
def step_impl(context):
    # Implementation depends on asserting the lock state and duration
    assert context.account.is_locked, "Account is not locked"
    assert context.account.lock_duration == 15, "Account lock duration is not 15 minutes"
@then('the system denies further withdrawal attempts')
def step_impl(context):
    # Implement check to verify the system denies further withdrawal attempts
    assert context.system.is_withdrawal_denied() is True, "The system did not deny further withdrawal attempts"
@given('I enter a valid PIN "{pin}"')
def step_enter_valid_pin(context, pin):
    # Assuming there is a page object method to enter PIN, e.g. context.page.enter_pin(pin)
    context.page.enter_pin(pin)
# Reused existing step for: When I select to withdraw an amount of 500
# Reused existing step for: And the system verifies sufficient balance
@then('the system dispenses cash of 500')
def step_impl(context):
    context.execute_steps('''
        Then the system dispenses the cash amount "500"
    ''')
# Reused existing step for: And the system updates my account balance accordingly
# Reused existing step for: And I do not request a receipt
# Reused existing step for: Then I do not receive a receipt

--- PAGE METHODS ---
# Use existing method: search_to_checkout
# Use existing method: enter_text
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: login
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: login
# Use existing method: login
# Use existing method: login
# Use existing method: attempt_login
# Use existing method: attempt_login
# Use existing method: attempt_login_for_target_account
# Use existing method: attempt_login_for_target_account
# Use existing method: enter_text
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout
# Use existing method: search_to_checkout

